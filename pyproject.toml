[project]
name = "linkedin-post-generator"
version = "0.1.0"
description = "LinkedIn post generator using Streamlit and OpenAI"
authors = [{ name = "Jacob Brain", email = "jacob@example.com" }]
dependencies = [
  "streamlit",
  "openai"
]

